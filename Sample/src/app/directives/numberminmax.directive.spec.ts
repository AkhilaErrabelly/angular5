import { NumberminmaxDirective } from './numberminmax.directive';

describe('NumberminmaxDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberminmaxDirective();
    expect(directive).toBeTruthy();
  });
});
